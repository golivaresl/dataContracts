dataContractSpecification: 0.0.1
id: gentera-ylc-da09t-001
name: Contrato Maestro YLC_DA09T
version: 1.0.0

servers:
  # Servidor para producción en AIDP usando el driver nativo de OCI
  prod_aidp:
    type: s3 # Spark interpreta s3/oci de forma compatible en AIDP
    endpointUrl: "oci://testDataCerts@idcuukoybpv7/"

models:
  YLC_DA09T:
    type: table
    externalData:
      format: csv
      # Nueva ruta nativa basada en tu URL de Ashburn
      location: "oci://testDataCerts@idcuukoybpv7/YLC_DA09T.CSV"
      delimiter: ";"
      header: true
    checks:
      - row_count > 0
    fields:
      BPARTNER:
        type: string
        description: "Interl.comercial"
        required: true
        maxLength: 10
      VALIDTO:
        type: string
        description: "Válido a"
        pattern: '^[0-9]{8}$'
      FP_RTGRMET:
        type: string
        description: "Proced.valoración"
        maxLength: 10
      FP_RTCRIT:
        type: string
        description: "Criterio diferenc."
        maxLength: 10
      RECORDMODE:
        type: string
        description: "Modo actualización"
        maxLength: 1
      VALIDFROM:
        type: string
        description: "Válido de"
        pattern: '^[0-9]{8}$'
      FP_RTPERMT:
        type: string
        description: "Autoriz.valoración"
        maxLength: 1
      FP_RTGRADE:
        type: string
        description: "Valorac."
        maxLength: 10
      FP_RTTENDY:
        type: string
        description: "Tendencia"
        maxLength: 2
      FP_RTDWHEN:
        type: string
        description: "Fecha entrada"
        pattern: '^[0-9]{8}$'
      TXTLG:
        type: string
        description: "Descripción expl."
        maxLength: 60
      CRM_PROD:
        type: string
        description: "Producto"
        maxLength: 32